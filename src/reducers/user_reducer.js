import {FETCH_TODOS} from "../actions/types";

const initialState = {
    firebaseUser: {

    },
};

export default (state = initialState, action) => {
    switch(action.type) {
        case LOGIN_USER: {


            return newState;
        }


        case REGISTER_USER: {

            return newState;
        }

        default:
            return state;
    }
};
